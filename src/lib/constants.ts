export const APP_NAME = 'ProRequests';

export const CHART_COLORS = {
  new: 'hsl(var(--chart-1))',
  inProgress: 'hsl(var(--chart-2))',
  review: 'hsl(var(--chart-3))',
  completed: 'hsl(var(--chart-4))',
} as const;